<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <title>ELS INCISIUS CENTRALS TEMPORALS A QUINA EDAD ERUPCIONEN?</title>
    <style>
        body {
            background-color: #C7F48E;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
        }

        #contenedor {
            display: flex;
            align-items: center; /* Alinea los elementos verticalmente en el centro */
        }

        #imagen-carie {
            width: 50%;
            max-width: 600px; /* Ajusta el tamaño según tus necesidades */
            margin-right: 20px;
        }

        .enfermedad-buttons {
            display: flex;
            flex-direction: column;
        }

        .enfermedad-button {
            background-color: transparent;
            color: #000;
            border: none;
            padding: 10px;
            margin: 5px;
            cursor: pointer;
        }

        .enfermedad-verde {
            background-color: #008000;
            color: #fff;
        }

        .enfermedad-rojo {
            background-color: #d9534f;
            color: #fff;
        }
    </style>
</head>

<body>
    <h1>ELS INCISIUS CENTRALS TEMPORALS A QUINA EDAD ERUPCIONEN?</h1>

    <div id="contenedor">
        <img id="imagen-carie" src="Crono1.png" alt="Foto Carie">

        <div class="enfermedad-buttons">
            <button class="enfermedad-button" onclick="cambiarColor(this, 'rojo')">A) 10 Mesos</button>
            <button class="enfermedad-button" onclick="cambiarColor(this, 'verde')">B) 7-8 mesos</button>
            <button class="enfermedad-button" onclick="cambiarColor(this, 'rojo')">C) Primer anys</button>
        </div>
    </div>

    <script>
        // Función para cambiar el color
        function cambiarColor(boton, color) {
            // Reiniciar todos los botones a color negro transparente
            var botones = document.getElementsByClassName("enfermedad-button");
            for (var i = 0; i < botones.length; i++) {
                botones[i].classList.remove("enfermedad-verde");
                botones[i].classList.remove("enfermedad-rojo");
            }

            // Poner el color correspondiente al botón clicado
            if (color === 'verde') {
                boton.classList.add("enfermedad-verde");
            } else if (color === 'rojo') {
                boton.classList.add("enfermedad-rojo");
            }

            // Cambiar el color del texto al hacer clic
            boton.style.color = color === 'verde';
        }

        // Función para reorganizar los botones aleatoriamente
        function reorganizarAleatoriamente() {
            var container = document.querySelector(".enfermedad-buttons");
            for (var i = container.children.length; i >= 0; i--) {
                container.appendChild(container.children[Math.random() * i | 0]);
            }
        }

        // Llamar a la función al cargar la página
        window.onload = reorganizarAleatoriamente;
    </script>

</body>

</html>
